<h1>Results for: {{category}}</h1>
<div>
  {{#each searchedProducts }}
  <h2>{{this.name}}</h2>
  <a href="/articles/{{this._id}}"><img width="150px" src="{{this.image}}" alt="{{this.name}}"></a>
  <p>{{this.format}}</p>
  {{/each}}
</div>