<h1>Highlighted Articles</h1>
<div>
  {{!-- IF USER CURRENT --}}
  {{#each highlighteds }}
    <h2>{{this.product.name}}</h2>
    <img width="150px" src="{{this.product.image}}" alt={{this.product.name}}>
    <h2>Price: {{this.article.price}} euros $</h2>
    <p>{{this.product.format}}</p>
    <p>{{this.market.name}}</p>
    {{!-- Edit
    Delete --}}
  {{/each}}
</div>